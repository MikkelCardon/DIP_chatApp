
.receivedChatMessage
    p= message.createdByUser
    p= message.text
    if userLevel === 3
        button.deleteMessageButton(data-userId= userId, data-chatId= chatId, data-messageId= message.id) remove
